NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Optional — if set, all AI features (match preview, banter report, bracket
# coach) call Claude Sonnet 4.5 instead of using deterministic templates.
# Without this key the app falls back to the template versions, which work
# fine and have zero per-request cost.
ANTHROPIC_API_KEY=
